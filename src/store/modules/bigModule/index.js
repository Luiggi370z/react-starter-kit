import actions from './actions'
import actionTypes from './actionTypes'
import reducer from './reducer'
import selectors from './selectors'

const bigModule = {
  actions,
  actionTypes,
  reducer,
  selectors
}

export default bigModule
